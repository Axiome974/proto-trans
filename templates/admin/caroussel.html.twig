{% extends 'admin/base_admin.html.twig' %}

{% block admin_body %}
    <h2>Caroussel </h2>
    {{ form(form) }}
    
    <div class="container">
        <div class="row">
            
            {% for image in images %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <img src="{{ asset('/uploads/images/')~image }}" alt="" class="img-fluid">

                        </div>
                    </div>
                </div>

            {% endfor %}
        </div>
            
    </div>
    
{% endblock %}
